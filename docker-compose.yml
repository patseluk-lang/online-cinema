services:
  web:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DJANGO_DB_PATH: /data/db.sqlite3
    ports:
      - "80:8000"
    volumes:
      - db-data:/data

volumes:
  db-data: